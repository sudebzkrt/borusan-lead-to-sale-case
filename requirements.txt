# local end-to-end run (Fabric already has pyspark; only the ai_product/ and data_gen/ deps matter there)
pandas>=2.0
numpy>=1.24
pyarrow>=14
pyspark==3.5.3
scikit-learn>=1.4
streamlit>=1.50
openai>=1.30
jupytext>=1.16
